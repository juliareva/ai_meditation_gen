# Install memcached which is a database for this project
brew install memcached

# Install tool to work with YouTube videos via console
brew install yt-dlp

# Install pyenv to manage python version on the computer
brew install pyenv

# Install the python version that supports XTTS
pyenv install -v 3.10.14

# Activate required Python version
pyenv global 3.10.14

# Install for audio
brew install ffmpeg